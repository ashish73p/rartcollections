# Copy this file to .env.local and fill in your key.
# NOTE: this key is inlined into the client bundle at build time and is therefore
# visible to anyone who loads the deployed app. Do not use a production key for a
# public deployment. See the "Security note" section in README.md.
GEMINI_API_KEY=your-gemini-api-key-here
